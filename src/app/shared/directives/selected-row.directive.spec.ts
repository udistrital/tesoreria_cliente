import { SelectedRowDirective } from './selected-row.directive';

describe('SelectedRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedRowDirective();
    expect(directive).toBeTruthy();
  });
});
